
  # Travel One web application

  This is a code bundle for Travel One web application. The original project is available at https://www.figma.com/design/h8vos32UFGVQiEiQyBSSIm/Travel-One-web-application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  